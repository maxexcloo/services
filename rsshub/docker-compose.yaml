networks:
  default:
    external: true
    name: caddy
services:
  rsshub:
    image: diygod/rsshub:chromium-bundled
    labels:
      caddy: ${WEBSITE_FQDN}
      caddy.reverse_proxy: "{{upstreams 1200}}"
    restart: unless-stopped
