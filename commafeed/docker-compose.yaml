networks:
  default:
    external: true
    name: caddy
services:
  commafeed:
    environment:
      CF_APP_PUBLICURL: ${URL}
    image: athou/commafeed
    labels:
      caddy: ${FQDN}
      caddy.reverse_proxy: "{{upstreams 8082}}"
    restart: unless-stopped
    volumes:
      - commafeed_data:/commafeed/data
volumes:
  commafeed_data:
