
services:
  dailydisplay:
    environment:
      CONFIG: '${jsonencode(service.config)}'
    image: ghcr.io/maxexcloo/dailydisplay
    ports:
      - ${service.port}:${service.port}
    restart: unless-stopped
