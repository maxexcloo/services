%{ for server in servers ~}
%{ if contains(server.flags, "docker") ~}
- glances:
    cputemp: true
    label: ${server.description} (${servers[server.location].description})
    uptime: true
    url: https://glances.${server.fqdn_internal}
    version: 4
%{ endif ~}
%{ endfor ~}
